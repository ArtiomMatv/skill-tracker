# Used for production / `vite build` + `vite preview` only.
# During `npm run dev`, the app uses same-origin `/graphql/` (Vite proxy); this is ignored in dev.
VITE_GRAPHQL_URL=http://127.0.0.1:8000/graphql/
